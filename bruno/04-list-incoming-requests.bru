meta {
  name: List incoming requests of B
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/users/{{userIdB}}/friend-requests?direction=incoming
  auth: none
}

assert {
  res.status: eq 200
}
