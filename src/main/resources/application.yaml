spring:
  application:
    name: tatakae-api
  profiles:
    default: dev

server:
  port: ${SERVER_PORT:8080}

# Documentation is off unless a profile turns it on: production must not expose the contract.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
